#!/usr/bin/env node
const fc  = require('../src');
const fn = "https://jeffz.solid.community/public/test4/testing.ttl"

fc.checkSession().then( sess => {
//    console.log(sess)
    fc.login().then( sess => {
        fc.checkSession().then( sess => {
             console.log(sess.webId)
        }, err => { console.log("checkSession #2 error : " + err) });
    }, err => { console.log("login error : " + err) });
}, err => { console.log("checkSession #1 error : " + err) });

